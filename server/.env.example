SECRET_KEY=replace-with-a-secure-random-value
JWT_EXPIRE=24h
PORT=8000

# Optional: only needed for the one-time MongoDB to SQLite import
MONGODB_CONNECTION_STRING=mongodb://localhost:27017/warmwishes
MONGODB_DATABASE=project-tracker
